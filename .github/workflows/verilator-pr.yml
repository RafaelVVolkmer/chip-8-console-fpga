# SPDX-FileCopyrightText: 2026 Rafael V. Volkmer <rafael.v.volkmer@gmail.com>
# SPDX-License-Identifier: GPL-3.0-only

---
name: Verilator PR

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: verilator-pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verilator-pr:
    name: PR RTL simulation and traceability
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y verilator yosys

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: validation/rust -> target

      - name: Run PR regression
        run: make ci-verilator-pr

      - name: Upload validation report
        uses: actions/upload-artifact@v4
        with:
          name: verilator-pr-validation-report
          path: reports/validation
